import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: 280,
  },
  input: {
    borderBottomWidth: 1,
    borderColor: '#aab3aa',
    marginBottom: 10,
  },
  labelText: {
    fontSize: 14,
    color: 'green',
  },
});

export default styles;

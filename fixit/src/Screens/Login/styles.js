import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  textHeader: {
    color: 'green',
    fontSize: 32,
    fontWeight: 'bold',
    paddingVertical: 280,
    textAlign: 'center',
  },
});

export default styles;

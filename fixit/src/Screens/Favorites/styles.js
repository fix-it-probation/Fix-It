import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  card: {
    alignSelf: 'center',
    marginTop: 20,
    marginBottom: 20,
  },
  searchBar: {
    alignSelf: 'center',
    marginTop: 24,
    marginBottom: 20,
  },
});

export default styles;
